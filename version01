<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <title>TheEndgame - Skeleton Demo</title>
  <style>
    body {
      margin: 0;
      padding: 0;
      background: linear-gradient(120deg, #191919 0%, #8B4513 100%);
      font-family: 'Georgia', serif;
      color: #fff;
      min-height: 100vh;
    }

    .header-container {
      display: flex;
      align-items: center;
      justify-content: center;
      gap: 30px;
      padding: 20px 0;
      background: rgba(20, 20, 20, 0.9);
      box-shadow: 0 6px 32px rgba(0,0,0,0.5);
      backdrop-filter: blur(7px);
      border-bottom: 2px solid #68411b;
      position: sticky;
      top: 0;
      z-index: 1000;
      font-size: 1.18em;
    }
    .nav-btn {
      position: relative;
      padding: 12px 24px 12px 44px;
      margin: 0 13px;
      border-radius: 16px;
      color: #f1c073;
      background: rgba(80,60,40,0.15);
      box-shadow: 0 2px 20px rgba(0,0,0,0.08);
      transition: transform 0.28s, color 0.22s, background 0.23s;
      cursor: pointer;
      border: none;
      outline: none;
      font-size: inherit;
      letter-spacing: 1.1px;
      font-family: inherit;
    }
    .nav-btn:hover, .nav-btn:focus {
      color: #fff;
      background: rgba(120,80,40,0.24);
      transform: translateY(-5px) scale(1.09);
      box-shadow: 0 8px 38px #8B4513c4;
    }
    .play-now-menu {
      position: relative;
      display: inline-block;
    }
    /* (No chess-set-img for now) */

    .top-box {
      display: flex;
      align-items: flex-end;
      column-gap: 7vw;
      justify-content: left;
      height: 48vh;
      min-height: 380px;
      background: linear-gradient(to right, #171717 0%, #8B4513 99%);
      padding-left: 10vw;
      padding-top: 46px;
      position: relative;
      z-index: 3;
    }
    .knight-placeholder, .second-placeholder {
      width: 106px;
      height: 180px;
      margin-bottom: 28px;
      background: linear-gradient(120deg, #222 30%, #745f45 100%);
      border-radius: 25%;
      box-shadow: 0 14px 68px #000a, 0 1px 18px #fffc2c16;
      display: flex;
      align-items: center;
      justify-content: center;
      font-size: 1.12em;
      font-family: Georgia, serif;
      color: #ffe2ba;
      letter-spacing: 2px;
      font-weight: bold;
      text-align: center;
      text-shadow: 1.5px 2.5px 6px #000a;
      transition: transform 0.28s, box-shadow
